[package]
name = "om-lifeos"
version = "1.0.0"
description = "Om LifeOS desktop application"
authors = ["Om LifeOS"]
edition = "2021"

[lib]
name = "om_lifeos_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
